---
- name: Remove generated ephemeral private key directory from the runner
  ansible.builtin.file:
    path: "{{ ephemeral_root_private_key_path | dirname }}"
    state: absent
  when: ephemeral_root_private_key_path | default('') | length > 0
  no_log: true
